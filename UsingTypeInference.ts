// UsingTypeInference

let userName;
userName = ['Siver', 'Surfer'];

let userId = (a: string, b: number) => a + b;

let target = document.getElementById('target');
target.onclick = (event) => event.button;